# 首轮安装与真机测试

当前为 experimental 版本；请使用更新后的 [完整测试步骤](TESTING.zh-CN.md)。

默认模式是主屏镜像，扩展桌面需要显式选择 `--backend virtual`。Mac 首次须安装 `libusb` 与 `pkg-config`。未连接设备或启动失败时不会自动等待手机插入，请处理原因后重新运行。

CI 构建通过不代表真机已通过，不承诺 25ms 性能。

[ConfigCrate](https://configcrate.com/)
